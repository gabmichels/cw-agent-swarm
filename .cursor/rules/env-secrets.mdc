---
description: 
globs: 
alwaysApply: true
---
Assume the `.env` file exists and contains most environment variables unless otherwise noted. Use `process.env` and validate critical variables at runtime. Do not hardcode secrets or rely on undefined envs silently.


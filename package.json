{
  "name": "crowd-wisdom-employees-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/agents/*",
    "packages/agents/chloe"
  ],
  "scripts": {
    "dev": "npm run -w=@crowd-wisdom/ui dev",
    "build": "npm run build:packages && npm run -w=@crowd-wisdom/ui build",
    "build:packages": "npm run -w=@crowd-wisdom/shared build && npm run -w=@crowd-wisdom/core build && npm run -w=@crowd-wisdom/memory build && npm run -w=@crowd-wisdom/agents-chloe build",
    "lint": "npm run -w=@crowd-wisdom/ui lint"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.0",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@8.15.0",
  "dependencies": {
    "cron": "4.3.0",
    "discord.js": "14.14.1",
    "tsup": "8.0.2",
    "zod": "3.22.4"
  }
}

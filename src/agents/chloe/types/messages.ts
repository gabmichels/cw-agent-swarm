/**
 * Type definitions for messages used in LangChain interactions
 */

import { BaseMessage } from "@langchain/core/messages";

/**
 * Alias for LangChain's BaseMessage type for easier reference
 */
export type LangChainMessage = BaseMessage; 
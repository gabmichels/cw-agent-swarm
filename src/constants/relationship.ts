/**
 * Constants related to relationships between memory items
 */

/**
 * Types of relationships between memory items
 */
export enum RelationshipType {
  CAUSES = 'causes',
  SIMILAR = 'similar',
  CONTRADICTS = 'contradicts',
  SUPPORTS = 'supports',
  PART_OF = 'partOf',
} 
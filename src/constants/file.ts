/**
 * Constants related to file attachments and document types
 */

/**
 * Types of file attachments for messages
 */
export enum FileAttachmentType {
  IMAGE = 'image',
  PDF = 'pdf',
  DOCUMENT = 'document',
  TEXT = 'text',
  OTHER = 'other',
} 
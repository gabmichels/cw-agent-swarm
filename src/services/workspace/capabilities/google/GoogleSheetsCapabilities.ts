import { ISheetsCapabilities } from '../interfaces/ISheetsCapabilities';
import { SheetsCapabilities } from '../SheetsCapabilities';

/**
 * Google Workspace Sheets Capabilities Implementation
 * Extends the existing SheetsCapabilities to implement the provider interface
 */
export class GoogleSheetsCapabilities extends SheetsCapabilities implements ISheetsCapabilities {
  // All methods are inherited from SheetsCapabilities
  // This class serves as the Google-specific implementation
} 